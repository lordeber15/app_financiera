# URL del backend FastAPI al que el proxy de Vite reenvía las llamadas /api/*
# en desarrollo (npm run dev / npm run dev:mobile). Cambia esto si el backend
# corre en otra máquina o puerto de la red.
API_PROXY_TARGET=http://localhost:8014
